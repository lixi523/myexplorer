# waydir_term

In-tree fork of [xterm.dart](https://github.com/TerminalStudio/xterm.dart) — a
terminal emulator and Flutter widget — © terminal.studio, MIT licensed (see
`LICENSE`).

Vendored into Waydir so the terminal emulator is under our control and builds
offline.
