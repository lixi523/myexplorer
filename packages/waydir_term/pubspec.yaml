name: waydir_term
description: In-tree fork of xterm.dart (MIT, © terminal.studio). Terminal emulator and Flutter widget used by Waydir.
version: 4.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  meta: ^1.3.0
  quiver: ^3.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
